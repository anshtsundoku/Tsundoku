# Cloudflare Pages SPA fallback — every route serves index.html so client-
# side routing works on direct loads (e.g. /d/football, /settings).
#
# API calls go cross-origin directly to the worker (see src/lib/api.js),
# so we don't need an /api proxy rule here.

/*    /index.html    200
